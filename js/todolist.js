let myStorage = window.localStorage;

function addTaskToList() {
    let task = document.getElementById("item");
    let itemList = document.getElementsByClassName("thelist");

    console.log(task);
    console.log(itemList.length);
    
}